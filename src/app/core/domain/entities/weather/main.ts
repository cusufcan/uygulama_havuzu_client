export class Main {
  constructor(
    public temp: number | null,
    public pressure: number | null,
    public humidity: number | null,
    public temp_min: number | null,
    public temp_max: number | null
  ) {}
}
